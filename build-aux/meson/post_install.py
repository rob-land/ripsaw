#!/usr/bin/env python3
# Stub: gnome.post_install() in meson.build already handles schemas, icon
# cache, and desktop database refresh. Keep this file as a hook point for
# anything we need beyond that (e.g. presets directory creation under XDG
# state on first run).

import sys

if __name__ == '__main__':
    sys.exit(0)
