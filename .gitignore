# Rust / Cargo
/target/
Cargo.lock.bak
**/*.rs.bk

# Meson
/build/
/builddir/
/_build/

# Flatpak
.flatpak-builder/
flatpak-build/
repo/

# Editor / OS
.vscode/
.idea/
*.swp
*~
.DS_Store

# Generated
/data/*.desktop
/data/*.metainfo.xml
/data/resources/*.gresource

# Secrets / local-only
.env
.envrc
*.local

# Large local-only test material (3D BD samples, mounted ISOs, etc.)
/samples/
/scratch/

# Reference spec PDFs (e.g. ITU-T H.264) — copyrighted, kept locally only
/docs/*.pdf

# JM ldecod default-output files, if it's ever run with cwd in the repo
/log.dec
/dataDec.txt
/test_dec.yuv
